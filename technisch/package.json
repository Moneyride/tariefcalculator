{
  "name": "overuurtje",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build-standalone.mjs && node build-netlify.mjs",
    "build:standalone": "node build-standalone.mjs",
    "build:netlify": "node build-netlify.mjs",
    "push:generate-keys": "node generate-vapid-keys.mjs",
    "shopify:register-webhooks": "node register-shopify-webhooks.mjs",
    "test": "node --test tests/*.test.js",
    "start": "python3 -m http.server 4173 --directory .."
  }
}
